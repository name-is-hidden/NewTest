text_dict = {text: ord(text) for text in input().split(', ')}

print(text_dict)

########################################################################################################################

# print({text: ord(text) for text in input().split(', ')})
