# Find how many hours are needed for an architect to finish his projects:

name = input()
projects = int(input())
hours = projects * 3            # Each project requires 3 hours to prepare

print(f"The architect {name} will need {hours} to complete {projects} project/s")
