# First in Last Out -> FILO or LIFO (Last in First Out

# Push:
# append() - pushes the element on top of the stack

# pop() - pops the last element (last inserted or top of stack)

# Peek:
# print(example_list[-1] - this return the first object in the stack (the last inserted)

# len()
